--- dashboard/src/components/ToolDetailPanel.tsx
+++ dashboard/src/components/ToolDetailPanel.tsx
@@ -4,6 +4,7 @@
 import { ToolStat, useToolMetrics, getToolDescription } from "@/lib/tools";
 import { motion, AnimatePresence } from "framer-motion";
+import { TOOL_EQUATIONS } from "@/lib/equations";
 
 interface ToolDetailPanelProps {
   selectedTool: ToolStat;
@@ -62,6 +63,7 @@
 
   const toolDesc = getToolDescription(metrics.tool_id);
   const explainer = selectedMetric ? METRIC_EXPLAINERS[selectedMetric] : null;
+  const equationObj = TOOL_EQUATIONS[metrics.tool_id];
 
   return (
     <div className="mt-6 p-6 border border-[var(--gold)] bg-[var(--background)]/60 rounded-md artisan-shadow">
@@ -142,6 +144,22 @@
         )}
       </AnimatePresence>
 
+      {equationObj && (
+        <div className="mt-6 p-5 border border-emerald-500/30 bg-emerald-950/20 rounded-md space-y-3">
+          <div className="flex justify-between items-center border-b border-emerald-500/20 pb-2.5">
+            <span className="text-[10px] font-black uppercase tracking-[0.3em] text-emerald-400">Mathematical Model</span>
+          </div>
+          <div className="space-y-4">
+            <div className="p-4 bg-black/40 rounded border border-emerald-500/10 overflow-x-auto custom-scrollbar">
+              <pre className="text-emerald-400 font-mono text-sm leading-relaxed whitespace-pre-wrap">{equationObj.math}</pre>
+            </div>
+            <p className="text-[11px] opacity-80 leading-relaxed font-mono text-emerald-200">
+              {equationObj.desc}
+            </p>
+          </div>
+        </div>
+      )}
+
       {toolDesc && (
         <div className="mt-6 border-t border-[var(--border-muted)] pt-6 grid grid-cols-1 md:grid-cols-3 gap-6">
